export const featureFlags = {
  // Disable server-side title mass updates during development/content work
  ENABLE_TITLE_UPDATES_ON_MOUNT: false,

  // Disable realtime leaderboard subscription to avoid frequent refetches
  ENABLE_LEADERBOARD_REALTIME: false,
};
